import time

MAX= 15

bd_pessoa = [
    ['Linda', 165 , 'feminino'],
    ['Amanda', 160 , 'feminino'],
    ['Ricardo', 176 , 'masculino'],
    ['Fátima', 161 , 'feminino'],
    ['Alan Delon',177, 'masculino'],
]

def cadastra_pessoa(bd):
    print("\n--- CADASTRO ---")
    nome = input("Digite o nome da pessoa: ")
    genero = input("Digite o gênero da pessoa: ")
    altura = float(input("Digite a altura da pessoa: "))

    pessoa = [nome, altura, genero]
    bd.append(pessoa)

    print("Pessoa cadastrada com sucesso!")
    time.sleep(1)

def comparacao_altura(bd):
    if not bd:
        print("Não há pessoas cadastradas.")
        return
    alturas = [p[1] for p in bd]
    menor = min(alturas)
    maior = max(alturas)
    pessoa_maior = [p[0] for p in bd if p[1] == maior]
    pessoa_menor = [p[0] for p in bd if p[1] == menor]

    print(f"A menor altura é {menor} cm e pertence a: {', '.join(pessoa_menor)}")
    print(f"A maior altura é {maior} cm e pertence a: {', '.join(pessoa_maior)}")
    time.sleep(2.5)

def media_altura_masculino(bd):
    alturas_homens = [p[1] for p in bd if p[2].lower() == 'masculino']
    if not alturas_homens:
        print("Não há homens cadastrados.")
    else:
        media = sum(alturas_homens) / len(alturas_homens)
        print(f"A média de altura dos homens é: {media:.2f} cm")
    time.sleep(2.5)

def numero_de_mulheres(bd):
    qtd = sum(1 for p in bd if p[2].lower() == 'feminino')
    print(f"O número de mulheres cadastradas é: {qtd}")
    nomes_mulheres = [p[0] for p in bd if p[2].lower() == 'feminino']
    print("Nomes das mulheres:", ", ".join(nomes_mulheres))
    time.sleep(2.5)


while True:
    print('-- Coletor de dados --')
    print("\n--- MENU ---")
    print("1 - Cadastrar pessoa")
    print("2 - Maior e menor altura")
    print("3 - Média da altura masculina")
    print("4 - Número de mulheres")
    print("5 - Sair")

    try:
        op = int(input("Digite sua opção: "))
    except ValueError:
        print("Por favor, digite um número válido!")
        continue

    if op == 1:
        cadastra_pessoa(bd_pessoa)
    elif op == 2:
        comparacao_altura(bd_pessoa)
    elif op == 3:
        media_altura_masculino(bd_pessoa)
    elif op == 4:
        numero_de_mulheres(bd_pessoa)
    elif op == 5:
        print("Encerrando o programa..")
        time.sleep(1.5)
    else:
        print("Opção inválida! Tente novamente.")
